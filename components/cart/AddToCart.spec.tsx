import { render } from "test-utils/testing-library-utils";
import AddToCart from "./AddToCart";

describe("AddToCart Component", () => {
	it("should do something", () => {
		// render(<AddToCart product={}/>);
	});
});
